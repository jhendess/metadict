///<reference path="../App.ts"/>

"use strict";

module MetadictApp {

    /**
     * A class with static properties for different event names in the core module.
     */
    export class SearchEvents {
        /**
         * Event which indicates that a new search should be enforced.
         * @type {string}
         */
        public static FORCE_SEARCH = "FORCE_SEARCH";
    }
}